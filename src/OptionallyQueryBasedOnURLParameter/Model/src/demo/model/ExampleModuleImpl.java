/* Copyright 2010, 2017, Oracle and/or its affiliates. All rights reserved. */
package demo.model;
import demo.model.common.ExampleModule;

import oracle.jbo.ViewObject;
import oracle.jbo.server.ApplicationModuleImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class ExampleModuleImpl extends ApplicationModuleImpl implements ExampleModule {
  /**This is the default constructor (do not remove)
   */
  public ExampleModuleImpl() {
  }

  public void filterDeptViewByDname(String dname) {
    ViewObject deptView = getDeptView();
    if (deptView.getWhereClause() == null) {
      deptView.setWhereClause("UPPER(dname) like UPPER(REPLACE(:TheName,'*','%'))||'%'");
      deptView.defineNamedWhereClauseParam("TheName",null,null);
    }
    deptView.setNamedWhereClauseParam("TheName",dname);    
    deptView.executeQuery();
  }
  public void clearFilterOnDeptView() {
    ViewObject deptView = getDeptView();
    String whereClause = deptView.getWhereClause();
    if (deptView.getWhereClause() != null) {
      deptView.setWhereClause(null);
      deptView.removeNamedWhereClauseParam("TheName");
    }
    deptView.executeQuery();
  }  
  /**Sample main for debugging Business Components code using the tester.
   */
  public static void main(String[] args) {
    launchTester("demo.model", /* package name */
      "ExampleModuleLocal" /* Configuration Name */);
  }

  /**Container's getter for DeptViewWithBindVariable
   */
  public DeptViewWithBindVariableImpl getDeptViewWithBindVariable() {
    return (DeptViewWithBindVariableImpl)findViewObject("DeptViewWithBindVariable");
  }

  /**Container's getter for DeptView
   */
  public DeptViewImpl getDeptView() {
    return (DeptViewImpl)findViewObject("DeptView");
  }
}
