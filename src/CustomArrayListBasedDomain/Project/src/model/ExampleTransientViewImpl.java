/* Copyright 2010, 2017, Oracle and/or its affiliates. All rights reserved. */
package model;

import oracle.jbo.server.ViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class ExampleTransientViewImpl extends ViewObjectImpl {
    /**This is the default constructor (do not remove)
     */
    public ExampleTransientViewImpl() {
    }

    protected void create() {
        super.create();
        // Wipe out all traces of a query for this VO
        getViewDef().setQuery(null);
        getViewDef().setSelectClause(null);
        setQuery(null);
        // Enable the VO to manage rows by view row key
        setManageRowsByKey(true);
        // Ensure no query will be performed
        if (getMaxFetchSize() != 0) {
          setMaxFetchSize(0);        
        }
    }
}
