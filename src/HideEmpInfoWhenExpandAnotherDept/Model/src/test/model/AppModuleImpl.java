/* Copyright 2010, 2017, Oracle and/or its affiliates. All rights reserved. */
package test.model;

import oracle.jbo.Key;
import oracle.jbo.Row;
import oracle.jbo.Session;
import oracle.jbo.ViewObject;
import oracle.jbo.server.ApplicationModuleImpl;

// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class AppModuleImpl extends ApplicationModuleImpl {
    /**This is the default constructor (do not remove)
     */
    public AppModuleImpl() {
    }

    /**Container's getter for EmpView1
     */
    public EmpViewImpl getEmpView1() {
        return (EmpViewImpl)findViewObject("EmpView1");
    }


    /**Sample main for debugging Business Components code using the tester.
     */
    public static void main(String[] args) {
        launchTester("test.model", /* package name */
      "AppModuleLocal" /* Configuration Name */);
    }

    /**Container's getter for Departments
     */
    public DeptViewImpl getDepartments() {
        return (DeptViewImpl)findViewObject("Departments");
    }

    /**Container's getter for Employees
     */
    public EmpViewImpl getEmployees() {
        return (EmpViewImpl)findViewObject("Employees");
    }

    protected void prepareSession(Session session) {
        super.prepareSession(session);
        // We will only be finding rows by key programmatically
        // in this view, so set the max fetch size to zero so it
        // won't try to query
        getEmployees().setMaxFetchSize(0);
        
    }
}
